# Lombok-generierte Methoden mit @lombok.Generated markieren,
# damit Coverage-/Analyse-Tools (JaCoCo etc.) sie automatisch ausschließen.
lombok.addLombokGeneratedAnnotation = true
config.stopBubbling = true
